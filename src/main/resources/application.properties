spring.application.name=docker-service

# The hostname 'docker-db-services' matches the service name in docker-compose
spring.datasource.url=jdbc:postgresql://docker-db-services:5432/docker_database
spring.datasource.username=postgres
spring.datasource.password=Manish@2009

# Hibernate logic
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Recommended for Postgres
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect